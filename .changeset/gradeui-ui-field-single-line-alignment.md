---
"@gradeui/ui": patch
---

`Field` (option layout) now centres the control against a single-line label; the top-aligned `mt-0.5` treatment only applies when a `Field.Description` makes the text block taller than the control. Fixes the visibly skewed checkbox-with-label row.
