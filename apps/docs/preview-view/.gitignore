# Build artifacts — regenerated by build-view.mjs (pnpm -F @gradeui/docs build:preview-view).
# The bundled HTML is emitted to apps/mcp-server/src/preview-view-html.ts, which IS committed
# so the MCP server builds standalone; these intermediates are not needed in git.
dist/
