/**
 * Blend mode — the industry-standard droplet. This is intentionally
 * the lucide `Droplet` glyph re-exported under its semantic Grade
 * name: the Figma master (`blend-mode`) is a 1:1 clone of lucide's
 * droplet vector, so aliasing keeps the geometry guaranteed-identical
 * instead of maintaining a copied path.
 */
export { Droplet as BlendMode } from "lucide-react";
