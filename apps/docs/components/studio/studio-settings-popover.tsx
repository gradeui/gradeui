// Deleted — contents subsumed by `studio-settings.tsx` (the Sheet).
// `RendererMode` + `UserTier` now live in studio-settings.tsx.
// Safe to delete this file from the host (the sandbox couldn't
// remove it due to filesystem ownership).
export {};
