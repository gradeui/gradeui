# Prototype audit, 20 Sep 2026

Read-only sweep by five agents, one per section, every finding then challenged by a second agent; only what survived is here. Nothing in this file has been acted on unless it says FIXED.

Fixed on the day, because each was plainly wrong rather than a decision:
the white screen when picking a template; the hub's "in the two days
after 3 Sep" claim, which counted the send day and the day before it;
"1 of your last 10 were", which now agrees with itself; and Tripadvisor,
spelled two ways in one section, now TripAdvisor everywhere.

Everything else is left for Ali. The two worth deciding first: whether
the hub card trends belong behind the contextual insights switch (with
it off, which is the default, the cards still editorialise), and what
"Review velocity up 30% on last month" should measure, since it compares
9 days against 31.

46 findings stand.

## Reviews hub

### copy: The Review Builder card claims "Bank Holiday Visitors brought 29 reviews in the two days after 3 Sep." The 29 reviews it counts are dated 2 and 3 Sep,

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/_option-a/page.jsx:183 (Review Builder card, live by default at http://localhost:3020/locations/minus-one-studios/reviews)
- What: The Review Builder card claims "Bank Holiday Visitors brought 29 reviews in the two days after 3 Sep." The 29 reviews it counts are dated 2 and 3 Sep, so 14 of them arrived the day BEFORE the campaign was sent, and the two days actually after 3 Sep hold 7 reviews between them. It is wrong one click away, on the page the card links to.
- Evidence: Template at page.jsx:183: `${h.spike.campaign} brought ${h.spikeDayCount} ${...} in the two days after ${h.spike.date}`. The number comes from /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/lib/reviews-data.ts:417: `spikeDayCount: spikeAt === null ? 0 : list.filter((x) => x.daysAgo === spikeAt || x.daysAgo === spikeAt + 1).length`. daysAgo counts backwards from TODAY, so spikeAt is 3 Sep and spikeAt + 1 is 2 Sep: the window is the send day and the day before it, never the days after. The generator agrees, reviews-data.ts:235 places spike rows at `spikeAt + (rand() < 0.7 ? 0 : 1)`. Read live off Review Manager, all 60 rows across its three pages: Sep 3 = 15, Sep 2 = 1
- Suggested: Either count the days after the send (and have the generator place the rows there), or reword to what the rows support, for example "29 reviews arrived around 3 Sep, when Bank Holiday Visitors went out". Whichever way, it has to survive someone opening Review Manager and sorting by date.

### copy: "Review velocity up 30% on last month" is not a velocity. It compares the 9 days of September so far against the previous 31 days as raw counts, so it

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/_option-a/page.jsx:182 (Review Tracker card trend) and /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/lib/review-summary.ts:247 (the "review velocity vs last month" tile)
- What: "Review velocity up 30% on last month" is not a velocity. It compares the 9 days of September so far against the previous 31 days as raw counts, so it understates a real surge here and, in the live product, would print a false decline for most of every month.
- Evidence: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/lib/reviews-data.ts:367-370 sets `daysThisMonth = TODAY.getDate()` (9), `thisMonth = list.filter((x) => x.daysAgo < daysThisMonth)` and `lastMonth` as the 31 days before that, then line 432 computes `monthChangePct = Math.round(((thisMonth.length - lastMonth.length) / lastMonth.length) * 100)`. For Minus 1 Studios that is 52 reviews in 9 days against 40 in 31 days, reported as +30%, where the like for like daily rate is more than four times higher. The registry glossary defines the term differently: "Review velocity = the rate of new reviews over time" (/Users/alastairdriver/Development/2026/ramp-ds/gradeui/packages/stud
- Suggested: Compare equal windows (last 30 days against the 30 before), which is also what the glossary's "rate over time" means. If the month to date comparison is wanted, label it as such and show both counts so nobody quotes the percentage.

### other: The four hub cards, which are the entire job of this page, are plain divs that navigate on mouse click only. No link, no keyboard focus, no href, so n

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/_option-a/page.jsx:204-210 (HubCard) with /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/components/goto-bridge.tsx:46-60
- What: The four hub cards, which are the entire job of this page, are plain divs that navigate on mouse click only. No link, no keyboard focus, no href, so no open in a new tab, no middle click, no status bar preview, and nothing for a screen reader to announce as a destination.
- Evidence: Live DOM for [data-hook="reviews-hub-inbox"]: tagName DIV, attributes class, data-density, data-hook, data-slot, data-tracking-el, data-grade-goto, 0 anchors inside, and 0 focusable elements inside all four cards (the DrillArrow is aria-hidden with tabIndex -1 by design). CardTitleLink renders a CardTitle div carrying data-bl-link, not an anchor. GotoBridge registers only `document.addEventListener("click", onClick, true)`, with no keydown path. Mitigation: the sidebar still lists Review Manager, Tracker, Builder and Showcase, so the pages are reachable, but the landing page itself is not usable from a keyboard. This is not covered by any entry in /Users/alastairdriver/Development/2026/ramp-
- Suggested: Put a real anchor on the card title with the resolved href (the card keeps its click), or give the card role="link" tabIndex={0} and add a keydown branch to GotoBridge. The pattern is prototype wide, so fixing it in GotoBridge and CardTitleLink fixes every promoted screen at once.

### invented-feature: With contextual insights OFF, which is the default and what the client sees, all four hub cards still carry a generated commentary line, including a c

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/_option-a/page.jsx:260-267 (CardFooter trend), against /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/lib/demo.tsx:57-60
- What: With contextual insights OFF, which is the default and what the client sees, all four hub cards still carry a generated commentary line, including a causal attribution sentence. Everything else in the insights layer disappears, so the hub is the one page where the off switch does not fully switch off.
- Evidence: The setting's own doc comment: "Contextual insights on the pages. Off strips every strip, chip, nugget, dialog and first-run band, so the product can be shot without them (Ali, 12 Sep). Off by default since 17 Sep". ReviewSummaryStrip and BeaconChip both open with `if (settings.insights === false) return null;` (components/review-summary.tsx), and they do vanish. The CardFooter trend has no such guard, so the default page still reads "Review velocity up 30% on last month." and "Bank Holiday Visitors brought 29 reviews in the two days after 3 Sep." I cannot tell whether this is intended: the trend was Ali's own ask on 11 Sep (page.jsx:257, "drop these numbers, what we want is a meaningful tre
- Suggested: Ask Ali which side of the switch the card trends belong on. If they stay visible, keep them to plain fact (counts, how long the oldest has waited) and move the attributed and editorialised lines behind the switch with the rest of the layer.

### ds-inconsistency: The hub hand-rolls its own DrillArrow (a DS Button restyled into a circular icon button) instead of importing the one the ledger names as the sanction

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/_option-a/page.jsx:190-202
- What: The hub hand-rolls its own DrillArrow (a DS Button restyled into a circular icon button) instead of importing the one the ledger names as the sanctioned workaround, so the same restyle now exists twice with two different data hooks.
- Evidence: The ledger entry "drill-arrow" in /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/lib/ds-changes.ts records the finding and names the workaround: "it was hand-rolled at every site before the proposal module exported one", workaround "The proposal module exports DrillArrow (solid and glass variants)". The module's version at /Users/alastairdriver/Development/2026/ramp-ds/gradeui/packages/studio/registries/brightlocal/lib/proposal-page.jsx:232 is the identical solid look (`pointer-events-none size-9 shrink-0 rounded-full p-0` plus `group-hover:bg-secondary/80`) and its comment claims "this lib version is the single interim definition every card now uses". The hub import
- Suggested: Import DrillArrow from "@brightlocal/proposal" and delete the local copy, or, if the hook name matters to a capture script, pass dataHook to the imported one.

## Review Manager and Reply templates

### copy: The reply composer ships an AI drafting feature with a hardcoded plan entitlement: a "Draft with AI" button, a running counter "3 of 3 AI drafts left 

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:925, 968-975 (AI_DRAFT_QUOTA at line 213)
- What: The reply composer ships an AI drafting feature with a hardcoded plan entitlement: a "Draft with AI" button, a running counter "3 of 3 AI drafts left today", and a full-blown quota alert that states a billing term and a reset mechanic. Nothing in the prototype backs either number.
- Evidence: Line 969: `Your plan includes ${AI_DRAFT_QUOTA} AI drafts a day. The allowance resets 24 hours after the first draft you generated today.` AI_DRAFT_QUOTA = 3, set at line 213 with no comment and no source. Grep across the whole app: "AI draft" appears only in this file, and "plan includes" appears in exactly two places, here and on the subscription page. The subscription page (app/(app)/account/subscription/page.tsx, PLANS: Track / Manage / Grow) never mentions an AI-drafts allowance, so this screen invents a plan term the billing screen contradicts by omission. The sibling screen already solved this the other way and wrote down why: apps/brightlocal/app/(app)/locations/[location]/reviews/tr
- Suggested: Drop the number and the reset sentence and follow the Tracker settings pattern: no quota in the copy, a line saying the allowance comes from the plan, and a link to /account/subscription. If the quota state has to be demonstrable, keep the counter as demo wiring but stop attributing it to "your plan". Also vary the canned drafts, or expect "is it actually reading the review?" in the room.

### invented-feature: Each auto-reply rule carries a per-rule run history: six hardcoded runs naming six reviewers, each with a source, a timestamp, a template name and a S

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/templates/page.jsx:245-261 and 1095-1145 ("View recent activity")
- What: Each auto-reply rule carries a per-rule run history: six hardcoded runs naming six reviewers, each with a source, a timestamp, a template name and a Sent/Failed badge. It is unattributed in the file's own provenance notes, its reviewers exist nowhere else in the app, and its dates contradict the inbox sitting one click away.
- Evidence: The file header credits the brief for the delay field specifically ("The v2 brief asks for a free-form delay in minutes", line 230) but the run history arrives only as "rules gained delayMinutes / enabled / scope plus a run history" (line 18), with no brief or audit cited. The six reviewers in RULE_RUNS (Priya Shah, Tom Whelan, Grace Okoro, Daniel Rees, Maja Nowak, Callum Frost) return zero hits anywhere else in apps/brightlocal: they are not in lib/reviews-data.ts and not in the inbox. The newest run is dated 2026-08-27 while the prototype's today is 9 Sep 2026 (lib/reviews-data.ts:19), so an enabled rule whose own row reads "Replies 1 hour after the review arrives" appears to have done not
- Suggested: Apply the 17 Sep decision here too: remove "View recent activity" and keep at most a last-fired line on the rule row. If it stays for the demo, the runs have to be derived from the inbox's own Auto-replied rows so the two screens tell the same story, and the newest one has to land near the prototype's today.

### invented-feature: The auto-reply rule editor offers an account-wide scope: "All locations in the account", helped by "Every location in the account, including ones adde

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/templates/page.jsx:210-228 (SCOPES) and 1178-1190 (the "Applies to" Select)
- What: The auto-reply rule editor offers an account-wide scope: "All locations in the account", helped by "Every location in the account, including ones added later." A rule set that way from one location's page changes behaviour for every other location, and the prototype has no surface anywhere that shows or manages it.
- Evidence: Grep for "All locations in the account" and "Every location in the account" across apps/brightlocal returns only these two lines in this one file, so the concept has no counterpart in any other screen, dataset or copy string. The comment at line 210 argues the design case rather than citing a source: "scope is new in v2. A rule written once for the whole account beats the same rule copied into forty locations", which reads as our reasoning, not the product's. Contrast the control immediately below it at line 229, where the brief is cited explicitly: "The v2 brief asks for a free-form delay in minutes." Mechanically it cannot work either: rules live in per-screen useState (line 1336, `useStat
- Suggested: Default to location scope and take the picker out until there is an account-level rules surface to manage it from. If it must stay for the story, say where an account rule is administered, and expect "who can set this, and what does it do to my other 39 locations?" from the client.

### other: Reviews from read-only sources can never leave "Needs action". They carry the Needs action chip and count toward the tab, but the panel renders no foo

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:997 (ReplyActions early return) and 2258-2286 (the footer is conditional on canReply)
- What: Reviews from read-only sources can never leave "Needs action". They carry the Needs action chip and count toward the tab, but the panel renders no footer at all for them, so there is no Skip reply and no way to clear one. The inbox's headline number can never reach zero.
- Evidence: Line 997: `if (!canReply(review)) return null;` and line 2258 `{canReply(active) ? (<DrawerFooter ...>) : null}`. SOURCES (lines 186-205) marks TripAdvisor, Yelp, Yahoo! Local, Apple Maps and Bing Places canReply:false, and lib/reviews-data.ts assigns status without consulting the source at all (reviewsFor, lines 336-345). Verified live on http://localhost:3020/locations/minus-one-studios/reviews/manager: the Apple Maps row "Used to be great. Something has changed and not for the better." (Sep 7, 2026) shows Needs action, and opening it gives the read-only alert with no footer (DOM check: `[data-hook="review-panel"] [data-slot="drawer-footer"]` is null). The source facet on this location cou
- Suggested: Either render the footer with Skip reply for read-only sources (the record is local, nothing is posted), or exclude read-only sources from the Needs action bucket and give them their own resting state. Someone will click Needs action, work the list, and ask why it stops at two.

### invented-feature: The only route out of the read-only state is a button that does nothing. It promises to take you to the review on Google, Yelp, TripAdvisor, Apple Map

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:840-850 (the "Open in {source}" button in the read-only AlertInfo)
- What: The only route out of the read-only state is a button that does nothing. It promises to take you to the review on Google, Yelp, TripAdvisor, Apple Maps or Bing, and it has no destination.
- Evidence: The file says so itself at lines 831-834: "PROTOTYPE GAP, the real control is an <a> carrying the review's permalink with target='_blank'; the seed rows have no per-review URL, so this is the affordance without a destination." Confirmed live: the element at `[data-hook="open-source-r10"]` is a BUTTON with href null, and there is no onClick anywhere in the JSX. Because the panel also renders no footer for these reviews (see the finding above), this dead button is the single interactive thing offered on that state.
- Suggested: Add a permalink to the seed rows and make it a real link, or drop the button and let the alert be text only. A control that says "Open in Apple Maps" and does nothing is the first thing a client will click in that state.

### other: Deleting a reply that has been posted to Google takes one click with no confirmation, while deleting a local reply template on the sibling screen gets

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:2213 (onDelete) and 2207-2212 (onEdit)
- What: Deleting a reply that has been posted to Google takes one click with no confirmation, while deleting a local reply template on the sibling screen gets a full AlertDialog. Editing a reply silently moves the review back to Needs action and changes the tab counts before anything has been typed.
- Evidence: Line 2213: `onDelete={() => setStatus(active.id, "needs", "")}`, fired straight from the "Delete reply" button in the panel footer (ReplyActions, lines 1005-1013), no dialog, no undo, and the drawer stays open. Verified live: opening a Manually replied review gives a footer of exactly ["Delete reply", "Edit reply"]. Compare templates/page.jsx:781-880, where deleting a template opens an AlertDialog that says "This cannot be undone", and is refused outright when a rule depends on it, with the reasoning written out at line 782: "destructive, unrecoverable in this prototype". A template is account-local; a reply is public on Google. Line 2207-2212: `onEdit` calls `setStatus(active.id, "needs")`,
- Suggested: Put Delete reply behind the same AlertDialog treatment the templates page uses, naming the source the reply will be removed from. Hold the Edit status change until the edited reply is sent, or state that the review returns to Needs action while being edited.

### ds-inconsistency: The most important input on the screen is a raw <textarea> wearing a hand-copied version of the DS input styling, while its twin on the Reply Template

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:953-959 (the reply composer)
- What: The most important input on the screen is a raw <textarea> wearing a hand-copied version of the DS input styling, while its twin on the Reply Templates page uses the DS Textarea. This is not in lib/ds-changes.ts, and the DS does ship a Textarea.
- Evidence: Line 953-959: `<textarea ... className="border-input placeholder:text-muted-foreground focus:border-ring bg-background w-full resize-y rounded-md border px-3 py-2 text-sm outline-none" />`. It is the only raw `<textarea>` left anywhere in apps/brightlocal (grep over app/**). The templates page imports `Textarea` from `@brightlocal/ui-components/textarea` (line 47) and uses it for the template body, and its comment at lines 683-686 states exactly why this pattern was rejected there: "Field/Input/Textarea, not a hand-rolled border-input class. The local fieldClass string was a copy of the DS's own input styling that could only ever fall behind it, and it could not express states the components
- Suggested: Swap it for the DS Textarea, matching the templates drawer (`rows`, `resize-y text-sm`, and a Field/FieldLabel if the "Your reply" line should become a real label). If something about the DS Textarea blocks it, that is a ds-changes.ts entry.

### copy: The generic send-failure copy commits BrightLocal support to chasing a third party on the customer's behalf.

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:476 (SEND_FAILURES.unknown.description)
- What: The generic send-failure copy commits BrightLocal support to chasing a third party on the customer's behalf.
- Evidence: Line 476: "If it fails a second time, contact support with the reviewer's name and the date and we will chase it with {{source}}." That is a service-level promise about Google, Facebook and whoever comes next, written into product UI. Nothing else in the prototype makes a support commitment of this shape. The rest of the SEND_FAILURES set is careful and mostly self-service (reconnect, wait, ask the listing manager), which makes this one line stand out. I cannot check BrightLocal's actual support policy, so this is a flag for whoever can.
- Suggested: Soften to what support can actually do, for example "contact support with the reviewer's name and the date so we can look into it", and let the support team say whether escalation to the network is something they offer.

### ds-inconsistency: The heaviest DS overrides on these two screens are absent from lib/ds-changes.ts, which is the ledger the client-facing /docs/changes page renders. Th

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:2022-2032 (the DataTable className), 1038-1090 (BOTTOM_DRAWER_STYLE, BOTTOM_DRAWER_HANDLE, DRAWER_WIDTH), and templates/page.jsx:862-877 (the plain destructive Button in place of AlertDialogAction)
- What: The heaviest DS overrides on these two screens are absent from lib/ds-changes.ts, which is the ledger the client-facing /docs/changes page renders. They are written up in the Studio registry's report instead, which the client does not see.
- Evidence: The DataTable is re-engineered through descendant selectors into its private DOM: `[&_table]:border-separate [&_table]:border-spacing-0 [&_thead_th]:sticky [&_thead_th]:top-[var(--th-top,0px)] [&_tbody_td]:border-b [&_tbody_tr:hover]:bg-muted/50 [&_tbody_tr:has([data-open-row])]:bg-accent overflow-visible [&>div]:overflow-visible` (line 2032). The Drawer takes inline `marginTop: 0 / maxHeight: 92svh` to beat a data-attribute selector (line 1038), a handle restyle keyed on `[&>div:first-child]` which the file itself calls out as breakable ("it is the one thing here that a DS refactor could silently break", line 1049), and a width override that has to repeat the `data-[vaul-drawer-direction=ri
- Suggested: Promote the DataTable sticky-header / row-highlight overrides, the three Drawer overrides and the AlertDialogAction variant gap into lib/ds-changes.ts with the same finding / workaround / ask shape as the existing 24, so the ledger the client reads matches the code they are looking at.

### ds-inconsistency: Both screens draw their own TripAdvisor logo, a redrawn approximation with the brand hex hardcoded, duplicated across two files. It is shown to the cl

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/manager/page.jsx:150-172 and templates/page.jsx:111-133 (TripAdvisorMark)
- What: Both screens draw their own TripAdvisor logo, a redrawn approximation with the brand hex hardcoded, duplicated across two files. It is shown to the client as if it were the real mark, and it is not in the ledger.
- Evidence: The manager header flags it at line 62: "TRIPADVISOR MARK IS A REDRAWN STAND-IN, must not ship; the official asset belongs in @brightlocal/icons." The component carries `data-ds-standin="brand-mark"` and literal fills `#00AF87`. It appears in the source facet menu and on any TripAdvisor row (the source facet counts 1 for this location). lib/ds-changes.ts has a `bing-icon` entry covering the missing Bing mark, but nothing for TripAdvisor, so a reader of the ledger would conclude the icon set is complete apart from Bing.
- Suggested: Either fall back to the Globe the way Bing and Yahoo do, so nothing on screen is a redrawn third-party trademark, or add a ds-changes.ts entry alongside `bing-icon` asking for TripAdvisor in the social set. The duplicate definition in two files should collapse to one either way.

## Review Tracker and settings

### other: The settings page for Minus 1 Studios (a London recording studio) shows a different, American business's data: matched listings on goo.gl/maps/farmbur

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:164-181, :320, :359 — live at /locations/minus-one-studios/reviews/tracker/settings
- What: The settings page for Minus 1 Studios (a London recording studio) shows a different, American business's data: matched listings on goo.gl/maps/farmburger, yelp.com/biz/farm-burger-asheville and tripadvisor.com/farm-burger, alert recipients alex@farmburger.com and manager@farmburger.com, and "United States" as the country lede that decides which directories are listed. The same card's Sharing copy correctly reads "for Minus 1 Studios" two scrolls below, so the mismatch is visible in one screenshot.
- Evidence: `const country = "USA";` with its own comment: "No location in the prototype's data carries a country yet, and the sample directory rows are a US business's, so the list is the US one." DIRECTORIES.USA rows carry the farmburger URLs; line 359 `useState(["alex@farmburger.com", "manager@farmburger.com"])`. "Farm Burger" appears nowhere else in the prototype (grep across apps/brightlocal returns only this file). ds/data/minus-one-studios.json:619 gives the real address as "24-46 Lisson Grove, NW1 6TZ", and every other location is UK (Harbour & Co Brighton/Hove/Worthing). The file already contains a DIRECTORIES.UK list with Yell and Thomson Local that would have been right.
- Suggested: Drive `country` off the location (it is UK for every seeded location, so default to UK rather than USA), and replace the farmburger URLs and emails with the location's own — the same way `locationName` is already read from `useProposalData()` in the Sharing copy.

### invented-feature: The directory list this page claims to control does not match the directories the Tracker actually charts. Settings lists Google, Facebook, Yelp, Trip

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:164-181 vs tracker/page.jsx:271-279 (ENGAGED_SOURCES) and lib/location-profiles.ts:74-80
- What: The directory list this page claims to control does not match the directories the Tracker actually charts. Settings lists Google, Facebook, Yelp, Tripadvisor, Better Business Bureau, Yellow Pages and Trustpilot ("5 of 7 directories watched"). The Tracker charts Google, Facebook, Yelp, TripAdvisor, Yahoo! Local, Apple Maps and Bing Places ("7 sources", and all seven appear in its source filter menu and its table view). So three directories carrying live review counts on the Tracker cannot be seen or switched off in the settings behind it, and three directories that hold no reviews anywhere in the prototype appear as monitorable.
- Evidence: Live table view on the Tracker: "Google 862, Facebook 120, Yelp 61, TripAdvisor 38, Yahoo! Local 17, Apple Maps 12, Bing Places 6, Total 1,116". Live settings page: "Better Business Bureau — Not being watched", "Yellow Pages — Not being watched", "Trustpilot — We could not find a listing for this business". grep for "Better Business Bureau" and "yellowpages" across apps/brightlocal returns only this file (one unrelated hit in notes/website-copy.md). The two numbers even collide: "5 of 7 directories watched" sits beside the Tracker's "7 sources", meaning different sevens.
- Suggested: Build the directory list from the same source of truth the Tracker uses (lib/location-profiles.ts sources, plus the unconnected ones the inbox already models), so a directory you untick here is a directory that leaves the chart. Drop BBB and Yellow Pages unless there is real evidence BrightLocal monitors them.

### other: The Timeline's dates are 23 days behind the rest of the prototype, so September never appears on a page whose header says the data is current to 9 Sep

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/page.jsx:255 — live on the Timeline card
- What: The Timeline's dates are 23 days behind the rest of the prototype, so September never appears on a page whose header says the data is current to 9 September. The card has its own `TODAY` constant set to 17 Aug 2026, while the reviews it plots come from lib/reviews-data.ts, whose TODAY is 9 Sep 2026. The reviews are counted in the right buckets; only the labels are wrong, which is the worse failure because nothing looks broken.
- Evidence: `const TODAY = new Date(2026, 7, 17);` (tracker page.jsx:255) against `export const TODAY = new Date(2026, 8, 9); // 9 Sep 2026, the prototype's "today"` (lib/reviews-data.ts:19). Live, with the page header reading "Last updated September 9, 2026": the Timeline bar reads "Monthly · Showing 12 of 37 months · Sept 25 to Aug 26", and switching to Weekly gives "1 Jun to 17 Aug". The most recent bucket ends 17 August.
- Suggested: Import TODAY from lib/reviews-data rather than redeclaring it, so bucketTick() labels agree with the daysAgo the data is measured in.

### other: Fortnightly is a dead option. The next-run calculation branches on "daily" and "monthly" and falls through to the weekly formula for everything else, 

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:325-347 (the nextRun IIFE), Frequency select at :492-503
- What: Fortnightly is a dead option. The next-run calculation branches on "daily" and "monthly" and falls through to the weekly formula for everything else, so choosing Fortnightly produces exactly the same Next run date as Weekly. The one control on the page that proves the schedule works does not react to a quarter of its own options.
- Evidence: `if (frequency === "daily") return iso(today + DAY); if (frequency === "monthly") {...} return iso(today + ((want - mondayFirst(today) + 7) % 7 || 7) * DAY);` — no fortnightly branch. Verified live: with Run day = Monday, Weekly gives "Next run: September 14, 2026" and Fortnightly gives "Next run: September 14, 2026". (Monthly correctly gives October 5, the first Monday.)
- Suggested: Add a fortnightly branch (next run day, then +7 if that falls in the off week), or drop Fortnightly if the legacy screens do not offer it — a frequency nobody can verify is worse than one fewer option.

### other: The Copy button on the share link does not copy. It only flips its own label to "Copied" for 1.8 seconds. In a client demo, someone will press it, pas

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:405-408 (copyLink) and :400 (shareUrl)
- What: The Copy button on the share link does not copy. It only flips its own label to "Copied" for 1.8 seconds. In a client demo, someone will press it, paste, and get whatever was on their clipboard before — a silent wrong answer rather than a visible gap. The link it pretends to copy is also a fabricated product domain, https://reports.brightlocal.com/r/8k2p1x, which appears nowhere else in the prototype.
- Evidence: `const copyLink = () => { setCopied(true); window.setTimeout(() => setCopied(false), 1800); };` — there is no navigator.clipboard call anywhere in the file. `const shareUrl = "https://reports.brightlocal.com/r/8k2p1x";`
- Suggested: Call navigator.clipboard.writeText(shareUrl) before setCopied(true). Separately, check the share host with BrightLocal before it goes in a screenshot, or use a neutral placeholder path on the prototype's own origin.

### ds-inconsistency: A raw <style> block in the page restyles three shipped DS components by data-slot — Field's gap above a radio group, InputList's row surface, font siz

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:240-270 (SPACING_FIXES), injected at the top of the page body
- What: A raw <style> block in the page restyles three shipped DS components by data-slot — Field's gap above a radio group, InputList's row surface, font size and weight, and InputListInput's height and font size — and none of the three is in lib/ds-changes.ts, the ledger BrightLocal reads. This is the largest un-ledgered DS deviation in the Tracker section, and it is the shape the no-restyling rule exists to catch: selectors reaching inside DS internals, from page source rather than app/custom.css.
- Evidence: The block targets `[data-slot="field"] > [data-slot="radio-group"]`, `[data-hook="recipients-list"] [data-slot="list"] > li > div` (background, font-size, font-weight) and `[data-hook="recipients-input"]` (min-height, height, font-size). Its own comment cites "the same 16px-against-14px mismatch logged as DS finding 3.8" — 3.8 is an item in packages/studio/registries/brightlocal/reports/RM-GET-REVIEWS-AND-WIDGETS-REPORT.md:485 about Input and Textarea, not InputList, and grep for "InputList", "Field" or "input-list" in lib/ds-changes.ts returns nothing. /docs and /docs/changes render DS_CHANGES as "N findings with the upstream ask", so these three are invisible to the client.
- Suggested: Add three ledger entries (Field's gap is tuned for one control not a group; InputList rows depend on --card not being remapped to white; InputListInput collapses in a flex column because it sets height not min-height) and move the CSS to app/custom.css alongside the other sanctioned patches.

### ds-inconsistency: Four DS restyles on the Tracker, each with a comment claiming it was written down, and none of them is in lib/ds-changes.ts: the sticky CardHeader rec

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/page.jsx:217 (TOOLTIP_ROW_GAP), :227-252 (STICKY_HEADER), :253 (STICKY_HEADER_STYLE), :852-856 (Progress)
- What: Four DS restyles on the Tracker, each with a comment claiming it was written down, and none of them is in lib/ds-changes.ts: the sticky CardHeader recipe (bg-card, border-b, -mt-3, rounded-t-[inherit], px-6 pt-4 pb-4), the inline override of CardHeader's two-row grid, the Progress bar's track and indicator recoloured with arbitrary-value classes, and a selector reaching into ChartTooltipContent's internal .flex-1 to add a gap.
- Evidence: STICKY_HEADER's comment: "That IS a className on DS chrome — deviation logged; the DS has no sticky affordance on Card." TOOLTIP_ROW_GAP's comment: "Logged as a DS finding; this is the local floor until it is fixed upstream." grep -i "sticky|CardHeader|Progress|ChartTooltip|Field" over lib/ds-changes.ts returns only two incidental mentions inside the global-layout-padding and content-body-max-width entries — no entry for any of these four. The Progress line is `indicatorClassName="bg-[var(--ds-tailwind-colors-neutral-500)]"` plus `className="h-2 flex-1 bg-[var(--ds-tailwind-colors-neutral-100)]"`, the same shape as the Badge restyle that IS logged as "the ONE sanctioned exception".
- Suggested: Add four entries with their upstream asks: a sticky variant (or a divider slot) on CardHeader; collapse CardHeader's grid to one row when it has one child; a neutral Progress colour so a magnitude bar need not be painted by hand; and a gap on ChartTooltipContent's label/value row. The .flex-1 selector is also brittle — ask for a data-slot on that row.

### copy: The "Once a day" alert option says "A single digest at 08:00 covering everything from the day before" — an invented server batch time — one card below

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:728, against the Next run copy at :548-556
- What: The "Once a day" alert option says "A single digest at 08:00 covering everything from the day before" — an invented server batch time — one card below the place where exactly that kind of detail was deliberately removed. On 17 Sep the Next run line dropped "06:00" because the API does not return it and the page cannot know it; the same reasoning applies verbatim to 08:00, and now the page quotes a schedule time in one card and refuses to in another.
- Evidence: Line 728: `caption: "A single digest at 08:00 covering everything from the day before."`. The Next run comment at :548: "NO CLOCK TIME (Ali, 17 Sep: 'Can we remove the time'). Margarita's review: the API does not return the next run, so the day can be worked out from the schedule but 06:00 is a server batch detail nothing on this page can know."
- Suggested: Cut the time: "A single digest each morning, covering everything from the day before." Or confirm the real digest time with BrightLocal and then put the time back in both places.

### copy: Two links promise an answer the destination does not give. "Manual runs come out of your plan's allowance. See what your plan includes" and the "No ma

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:561-564 ("See what your plan includes") and :527-534 (the "See plans" action on the rejection alert)
- What: Two links promise an answer the destination does not give. "Manual runs come out of your plan's allowance. See what your plan includes" and the "No manual runs left" alert's "See plans" both go to /account/subscription, which never mentions report runs or a manual-run allowance. A user who hits the rejection has no way to find out how many runs they get or how to get more.
- Evidence: Both links point at /account/subscription. grep for "manual run", "ad-hoc" or "allowance" across app/(app)/account/subscription/page.tsx and lib/plans.ts returns nothing (lib/plans.ts's only "run" is "Run comprehensive local SEO audits"). The file's own comment concedes it: "THE ALLOWANCE HINT. No number: the API gives no quota and the plans table has no manual-run figure to quote, so it says where the number lives instead of making one up." The run-state model is also marked "ASSUMPTION, demo only: one manual run is left".
- Suggested: Either add a manual-runs row to the plan comparison on /account/subscription so the link pays off, or soften the copy to not promise a figure ("Manual runs are limited by your plan") and point "See plans" at whatever page BrightLocal actually publishes the quota on.

### other: The table view of Review performance drops the "Facebook recommendations" sub-heading the chart view has, so it lists "5 star … 1 star, Recommended 10

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/page.jsx:865-899 (the ratings Table branch)
- What: The table view of Review performance drops the "Facebook recommendations" sub-heading the chart view has, so it lists "5 star … 1 star, Recommended 106, Not recommended 14" as one continuous column under the header "Rating" — exactly the reading the heading was added to prevent, that 106 and 14 are a sixth and seventh rating on the same scale. The same table then totals all seven rows into 1,116 under a Rating column, while the card's own average (4.7) is star-only.
- Evidence: Live table view: "Rating / Reviews · 5 star 836 · 4 star 93 · 3 star 21 · 2 star 13 · 1 star 33 · Recommended 106 · Not recommended 14 · Total 1,116". The chart branch's comment at :822: "SAY WHOSE THUMBS THESE ARE (Ali, 7 Sep: 'we need to add a title above the thumbs up down to show its facebook related'). The list ran 5 star down to 1 star and then two thumb rows with nothing between them, so 106 and 14 read as a sixth and seventh rating on the same scale."
- Suggested: Give the table the same break — a spanning row reading "Facebook recommendations" at the first non-star bucket (the same `bucket.kind !== "star" && activeBuckets[i-1]?.kind === "star"` test the chart uses), so the two views say the same thing.

### copy: The Connect Facebook dialog states BrightLocal's OAuth permission scopes as fact: "We only ask for reviews and the ability to reply to them. We never 

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/settings/page.jsx:917-920 (the Connect Facebook dialog's AlertInfo)
- What: The Connect Facebook dialog states BrightLocal's OAuth permission scopes as fact: "We only ask for reviews and the ability to reply to them. We never post to your Page." That is a commitment about what the product requests from Meta, in a modal a client will read as product copy. Nothing in the repo sources it, and it is the kind of line that has to be right rather than plausible.
- Evidence: `<AlertInfo dataHook="connect-facebook-note" description="We only ask for reviews and the ability to reply to them. We never post to your Page." />`. The surrounding comments source the dialog's existence to the audit's §3.1.3 Facebook point, but nothing sources the scope claim. The file is otherwise careful about this — the match badges and the white-label toggle were both cut because "the API sends no status" and "nothing here knows it".
- Suggested: Either get the actual scope list confirmed and quote it, or replace the sentence with something the prototype can stand behind ("You choose the Page, and you can disconnect at any time").

### copy: Tripadvisor is spelled two ways across the two pages of this one section: the Tracker's charts, legend, filter menu and tables say "TripAdvisor", the 

- Where: apps/brightlocal/app/(app)/locations/[location]/reviews/tracker/page.jsx:276 vs settings/page.jsx:169 and :178
- What: Tripadvisor is spelled two ways across the two pages of this one section: the Tracker's charts, legend, filter menu and tables say "TripAdvisor", the settings page's directory list says "Tripadvisor". The brand's own styling since 2020 is Tripadvisor, so the Tracker is the one that is wrong, and the two sit one click apart.
- Evidence: tracker/page.jsx:276 `{ id: "tripadvisor", name: "TripAdvisor", stars: 38 }` (and the same string in lib/location-profiles.ts for every location); settings/page.jsx:178 `{ id: "tripadvisor", name: "Tripadvisor", ... }`. Both confirmed on the live pages.
- Suggested: Settle on "Tripadvisor" in lib/location-profiles.ts and the Tracker's STARTER/ENGAGED source lists so every screen matches the brand and each other.

## Review Builder

### FIXED, 20 Sep. other: Opening any row in the "Pick a template" step white-screens the entire app. Because that row is the only live route into the wizard's name / ask / fee

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:1087 and :3485 (live: Review Builder > New campaign > Start from a template > click any row)
- What: Opening any row in the "Pick a template" step white-screens the entire app. Because that row is the only live route into the wizard's name / ask / feedback / channel / email / SMS / reminder steps, it also means a campaign's message wording can never be reached or edited in the running prototype: the settings page has four rows (name, rate-first, rating type, how it is sent, review sites) and "Review and send" jumps straight to recipients.
- Evidence: templateConfig() strips sites from every template: `const CAMPAIGN_ONLY = ["channel", "sites", "columns", ...]` (line 649, per the comment 'Ali, 7 Sep: "templates dont include review sites"'). The picker's peek panel then calls `campaignSummary(t.config)` (3485), and campaignSummary does `rows.push({ k: "Review sites", v: config.sites.map((s) => siteById(s.site).label).join(", ") })` (1087) on a config that has no `sites`. Reproduced twice live at localhost:3020: the page renders "This page couldn't load" and the console shows `TypeError: Cannot read properties of undefined (reading 'map')`. The only other caller of go("name") is step "start" (2924), which nothing ever sets (`useState("setup
- Suggested: Guard campaignSummary with `(config.sites ?? [])`, and drop the "Review sites" row from the template peek altogether: the Public review section's own note already says "The review sites are chosen on the campaign, not here". Then re-walk name -> ask -> feedback -> email/SMS, because none of those steps have been seen in the live app since the settings-page rework.

### copy: The only thing the live create flow says about "Ask for a rating first" is "Unhappy feedback comes to you first, not straight to Google." That is the 

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:2724 (live, campaign settings page) vs :3620-3625 (unreachable)
- What: The only thing the live create flow says about "Ask for a rating first" is "Unhappy feedback comes to you first, not straight to Google." That is the sentence a review-gating feature would use, and it is the first line a client reads on the create screen. The safeguard note that explicitly denies gating is on a step nobody can reach.
- Evidence: Verified live: the settings page shows the toggle plus that one line and no alert. The anti-gating AlertInfo ("Everyone who responds sees the same review page, whatever they scored. Sending only your happy customers to a review site is called review gating...", 3622) sits inside StepBody's `if (step === "ask")`, and step "ask" is only reached by walking on from "name", which is behind the crashing template row. The file's own assumption 4 says: "REVIEW GATING. Audit section 3.3.X flags the legal exposure ... (FTC 16 CFR 465.7). This screen therefore has NO branch that routes low scores away from the review sites".
- Suggested: Move a one-line version of the gating note onto the settings row beside the toggle, and reword the description so it neither names Google nor implies a diversion, e.g. "Customers rate you first and tell you why. Everyone is then invited to leave a public review."

### invented-feature: A "Scheduled" campaign state, with its own badge, its own banner and a "Cancel send" menu item, but nothing anywhere in the flow that can set a send d

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:1205-1213, :2102, :5578-5583 (seed campaign c6 "Autumn Half Term", line 566)
- What: A "Scheduled" campaign state, with its own badge, its own banner and a "Cancel send" menu item, but nothing anywhere in the flow that can set a send date or time. The banner also promises "you can still change the messages", and there is no route to the messages.
- Evidence: No date or time control exists anywhere in the 7,028-line file; the send step's primary is "Send now" and the standing step's is "Put live" / "Launch kiosk" (continueLabel, 3392-3401). The state table's own comment says "NOTHING SPECIFIES THESE. The RM Design Brief lists what Review Builder does but no states; the audit only says 'once live, each campaign has a campaign insights page'; the legacy screens show no state chips. Six is a design proposal". Header assumption 2 only sanctions "Live / Draft / Stopped" as the audit's proposal, so Scheduled is a step beyond even that.
- Suggested: Either add a "send later" date/time to the send step, or demote Scheduled to a seeded demo state and cut the "you can still change the messages" clause from the banner until there is a way to change them.

### invented-feature: A per-campaign brand accent colour: six swatches that remap --primary on every customer-facing surface (emails, feedback page, review page, kiosk). No

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:1240-1330 (BRAND_COLOURS, brandSurface, BrandPicker); live at Templates > edit a template > General > Accent colour
- What: A per-campaign brand accent colour: six swatches that remap --primary on every customer-facing surface (emails, feedback page, review page, kiosk). Nothing else in the prototype has a branding model, and the client never asked for one.
- Evidence: The upstream report states it plainly: "A campaign has an accent colour, six DS ramp tokens with grey first and default, remapping --primary on the customer surfaces. Nothing in the brief specifies a branding model for Get Reviews; this is the smallest one that stops the customer seeing BrightLocal's green" (RM-GET-REVIEWS-AND-WIDGETS-REPORT.md). The source comment cites Ali 2 Sep as a QUESTION, not a requirement: "is there a need from the previous get reviews to have some kind of colour palette or theme?". grep for `brandColor` hits only the four Review Builder layout variants and nothing else in the app.
- Suggested: Present it to the client as a proposal, flagged as such, or park it. If it stays, reconcile the two copies of the control: a CAMPAIGN cannot set a colour or a logo in the live flow at all (SetupBody has only name / rate-first / rating type / channel / sites), yet every customer preview renders one.

### invented-feature: A full card-capture checkout inside the campaign wizard: card number, expiry, security code, a "Pay $25.00" button and a "Payment successful" confirma

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:4745-4830 (SMS credit packages > Checkout > Payment successful)
- What: A full card-capture checkout inside the campaign wizard: card number, expiry, security code, a "Pay $25.00" button and a "Payment successful" confirmation. It is the only payment surface in the whole prototype, and the prices are in USD on a UK location whose contact-country default is UK.
- Evidence: grep for `card-number-input` / "Payment successful" / "Checkout" across apps/brightlocal hits only the four Review Builder variants; there is no account or billing page anywhere else in the app. The file's own comment concedes "Card fields are display only in this prototype and take no real input" (4744). CREDIT_PACKAGES are "$12.50 / $25.00 / $50.00 / $125.00" (383-388) while blankCampaignDraft defaults `country: "UK"` at 2 credits per text. Header assumption 3 flags only the rates and the 5c price, not the act of taking a card here.
- Suggested: Replace the checkout with a link out to account billing ("Top up credits" opening the billing area) and keep the shortfall warning, which is the part that earns its place. Price in the location's currency, or drop the prices until the commercial check in assumption 3 comes back.

### invented-feature: Two attribution claims the product may not be able to make: a per-campaign "Rating impact" star-rating delta, and "Reviews gained" whose own tooltip a

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:5194-5211 (Results tiles; live-verified on Season Pass Holders: "Rating impact +0.2")
- What: Two attribution claims the product may not be able to make: a per-campaign "Rating impact" star-rating delta, and "Reviews gained" whose own tooltip admits the link is inferred from timing.
- Evidence: "Rating impact" appears nowhere else in the prototype (grep for `impact` across app/, components/, lib/ excluding builder/ returns nothing), so it has no counterpart in Review Tracker where rating-over-time lives. Its tooltip reads "Rating change from this campaign's reviews only" and the neighbouring tile reads "New reviews this campaign brought in, matched on timing". The seed comment at 484 says "Numbers are invented demo data for the walkthrough, not captured from the live product." I cannot check the live BrightLocal product, so this is flagged as a question rather than a confirmed invention.
- Suggested: Ask the client whether campaign-level review attribution exists. If it is timing-based, say so on the tile face ("estimated") rather than only in a tooltip; if it does not exist, drop Rating impact and keep Reviews gained with the caveat visible.

### copy: A note to ourselves is shipped as product help text under the Accent colour swatches: it quotes the brief and names BrightLocal in the third person, i

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:3538-3541
- What: A note to ourselves is shipped as product help text under the Accent colour swatches: it quotes the brief and names BrightLocal in the third person, in a field description a BrightLocal user would read.
- Evidence: `<FieldDescription dataHook="brand-colour-desc">The colour of the buttons on your customers' pages and emails. Nothing in the brief specifies a branding model for Review Builder, so this is the smallest one that stops the customer seeing BrightLocal's green.</FieldDescription>`. It sits on the wizard's name step, which is currently behind the template-row crash, so it is not on screen today, but it will be the moment that is fixed.
- Suggested: Cut everything after the first sentence. The rationale belongs in the report, where the same words already are.

### ds-inconsistency: DataTable is substantially restyled from the call site with no entry in the ds-changes ledger: both of its scroll containers are cleared, border-colla

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:1751-1772 (TABLE_LOOK), applied to the campaigns, templates and feedback tables
- What: DataTable is substantially restyled from the call site with no entry in the ds-changes ledger: both of its scroll containers are cleared, border-collapse is switched to separate, every row rule is repainted per cell, first/last cell padding is reset, and a sticky header is injected via a custom --th-top variable.
- Evidence: The override string itself, and its own comment: "overflow-visible IS LOad-BEARING, and TWICE: DataTable nests an inner `relative overflow-auto` div inside its own `overflow-auto` root, and EITHER of them captures a sticky <th>" ... "border-separate IS ALSO LOad-BEARING. A sticky <th> does nothing inside a `border-collapse: collapse` table". Neither apps/brightlocal/lib/ds-changes.ts nor packages/studio/registries/brightlocal/DS-AUDIT.md contains any DataTable entry (grep: no hits).
- Suggested: Add a ds-changes.ts entry: finding "DataTable's nested overflow containers and collapsed borders make a sticky header row impossible", workaround "the TABLE_LOOK override string on three tables", ask "a stickyHeader that works, or a way to opt out of the inner scroll container".

### ds-inconsistency: Progress has its track and indicator painted with arbitrary bg-[var(--ds-tailwind-colors-neutral-200)]-style utilities in three places with no ledger entry, and the

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:5541, :5856, :5916 (restyles) and :5928-5945 (the thumbs bars)
- What: Progress has its track and indicator painted with arbitrary bg-[var(--ds-tailwind-colors-neutral-200)]-style utilities in three places with no ledger entry, and the justification comment is now factually wrong. Separately, the thumbs card is the one bar set still wearing the DS's bright green and red that Ali asked to be neutralised, so one campaign page contradicts the two beside it.
- Evidence: Comment at 5836: "THE DS COLOUR ENUM IS BROKEN ... MEASURED on the live render only green paints: yellow and red come back rgba(0,0,0,0)". Live check on Season Pass Holders contradicts it: the "Thumbs up or down" card draws a bright green and a bright red bar (the thumbs branch still passes `color={row.color}`), while the funnel and star bars on the same page are neutral. The star branch's own comment says "Neutral like every other ratings bar (Ali, 17 Sep: this 'still used the bright green')". No Progress entry exists in ds-changes.ts or DS-AUDIT.md.
- Suggested: Neutralise the thumbs bars the way the star and funnel bars were, re-measure the DS colour enum and correct or delete the stale comment, and log the real gap in ds-changes.ts: "Progress has no neutral value-bar colour; its colour enum is green/red/orange/yellow only."

### ds-inconsistency: A raw <style> block in the page shrinks every DS Input, Textarea and Select inside the template editing sheet to 13px, duplicating the same rule that 

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:3292-3300
- What: A raw <style> block in the page shrinks every DS Input, Textarea and Select inside the template editing sheet to 13px, duplicating the same rule that ds/wizard-shell.jsx applies to the wizard. Neither is in the ledger, so a reviewer comparing this screen with the rest of the product sees two different form-control sizes with nothing written down about why.
- Evidence: The block sets `[data-hook="section-sheet"] [data-slot="input"|"textarea"|"select-trigger"|"select-value"] { font-size: 0.8125rem }`, with the comment "That is why the sheet's fields came out a size larger than the same fields on the page (Ali, 7 Sep: 'the size of the text inputs varies'). Same rule, re-scoped to the sheet." The origin rule is WIZARD_TYPE_SCALE at apps/brightlocal/ds/wizard-shell.jsx:101, which also drops CardTitle to 18px. The underlying DS problem is written up as section 3.8 of RM-GET-REVIEWS-AND-WIDGETS-REPORT.md ("Input and Textarea ship at 16px against 14px labels") but ds-changes.ts, the ledger the app itself points at, has no entry.
- Suggested: Add the entry to ds-changes.ts with the 16px-vs-14px measurement and the ask (pair the control size with the label size, or expose a size prop), and reference it from both the wizard shell and this sheet so the deviation is one logged decision rather than two undocumented style tags.

## Review Showcase

### invented-feature: The Widget Design panel was just rebuilt control for control from screenshots of the real product (commit ee5f692, "I've been told to make it function

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:3208-3300 (the Animation AccordionItem) and CarouselWidget at :1370-1395
- What: The Widget Design panel was just rebuilt control for control from screenshots of the real product (commit ee5f692, "I've been told to make it functionally the same"), and the Mode control and the "Reviews by BrightLocal" line were deleted for not existing in the product. The Animation group (Loop, Autoplay, Every N seconds, Show arrows, Show dots) survived that cut and is ours. On a screen now presented as parity with the product it is the one group a BrightLocal product person can point at and say we made it up.
- Evidence: The comment above the group: "KEPT, NOT REBUILT. The real Widget Design screen has no carousel settings, but our Carousel showcase is a carousel and its loop, autoplay and controls are real behaviour, so they stay where they were rather than being deleted along with Mode and the branding line. ASSUMPTION: Ali may want these somewhere else, or gone." The autoplay itself is hand-rolled: "The DS ships embla-carousel-react but not the autoplay plugin, so this is the plugin's behaviour in an effect" (:1353). I cannot check the live product's carousel options, so whether the published widget can autoplay at all is unknown.
- Suggested: Ask BrightLocal what the live carousel actually offers. If it offers nothing, cut the group (the carousel keeps loop on, autoplay off, both controls on, which is what it looks like today) or move it out of Widget Design and label it as proposed, so the panel can be described as parity without an asterisk.

### copy: The Embed sheet hands over a copyable snippet on BrightLocal's production domain with a fabricated 40 character key and a fabricated numeric id, then 

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:985-1017 (embedId, embedKey, embedSnippet) and the steps in EmbedCode at :2313-2356
- What: The Embed sheet hands over a copyable snippet on BrightLocal's production domain with a fabricated 40 character key and a fabricated numeric id, then tells the reader to paste it into their site and publish. Nothing in the UI says the key is a placeholder, so anyone in the room who follows the three steps publishes a dead script tag pointing at www.local-marketing-reports.com.
- Evidence: ":985: "The numeric id and the 40-character hex key are ILLUSTRATIVE OF THE SHAPE: both derived deterministically from the showcase id by a small FNV-style hash... In reality both are issued by the API." The steps read "Copy the code. / Paste it into the page's HTML where the reviews should appear... / Publish the page." and the Copy button copies the whole snippet.
- Suggested: Keep the real shape but make the key unmistakably a placeholder (for example a visibly templated key, or a one line note above the code saying the real key is issued when the showcase is saved). Same for the JSON feed URL at :1001.

### other: A design preset rewrites every design key except the title, including things that are not a look: the Layout numbers (max height, columns, how many re

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:662-694 (PRESET_KEYS, presetOf) and applyPreset at :2853
- What: A design preset rewrites every design key except the title, including things that are not a look: the Layout numbers (max height, columns, how many reviews to show) and the Reviews group's Schema, name, site icon and date format. So picking Classic to change the look silently re-ticks "Add business details to Schema" and resets the date format, and moving to two columns flips the preset tile to Custom.
- Evidence: The comment at :674 flags it: "ASSUMPTION (Ali, 20 Sep: 'picking a preset sets every control below to that preset's values'): that is read literally, so a preset owns EVERY control in the four groups, not just the styling... So picking Classic re-ticks 'Add business details to Schema' and resets the date format. Schema is an SEO decision rather than a look, and nobody would expect a look to switch it back on. Both readings are defensible and this one is Ali's words taken at face value. Flag."
- Suggested: Put the question to BrightLocal: does their preset carry the layout numbers and the Reviews content toggles, or the styling alone? Schema in particular is the one that can quietly change a customer's markup.

### ds-inconsistency: The DS CarouselDots is replaced wholesale by an injected stylesheet: its border, white capsule background and padding are zeroed, each dot button beco

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:1323-1352 (CAROUSEL_DOTS_STYLE), injected at :1392
- What: The DS CarouselDots is replaced wholesale by an injected stylesheet: its border, white capsule background and padding are zeroed, each dot button becomes a 24px square, and the visible dot is redrawn by a ::before pseudo element. That is a restyled DS component and there is no entry for it in lib/ds-changes.ts (the ledger has no carousel entry at all), so the upstream ask never reaches BrightLocal.
- Evidence: The comment at :1323: "The DS CarouselDots draws each dot AS the button (size-3, a w-9 active pill, in a bordered white capsule) and has no size prop, so this scoped sheet restyles it." Confirmed in the shipped dist (node_modules/@brightlocal/ui-components/dist/carousel.js): the dots container is "border-border ... inline-flex items-center justify-center gap-2 rounded-full border bg-white px-2.5 py-2" and each dot is "size-3 rounded-full" with a "w-9" active state.
- Suggested: Add a ds-changes.ts entry (finding, workaround, ask) asking for a size or density variant on CarouselDots and a bare, un-capsuled mode, with a hit area separate from the dot.

### ds-inconsistency: Two real DS gaps are now load bearing on this screen and neither is in the ledger. There is no DS colour picker, so seven colour fields are a hand-rol

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:2647-2695 (ColorField) and :2580-2620 (SliderField)
- What: Two real DS gaps are now load bearing on this screen and neither is in the ledger. There is no DS colour picker, so seven colour fields are a hand-rolled composition of Input plus a swatch button plus a Popover holding the browser's own input[type=color]. And the DS Slider cannot be labelled: it spreads id onto the Radix Root, which renders a span, so FieldLabel's htmlFor binds to nothing and the two sliders are held together by aria-labelledby.
- Evidence: ":2647: "ONE COLOUR FIELD, USED BY ALL SEVEN COLOURS. The DS has no colour picker, so this is the DS Input carrying the hex, plus a square swatch button in the group's end addon that opens a DS Popover holding the browser's own <input type=\"color\">." :2586: "Checked in the DS dist: Slider spreads `id` onto the Radix Root, which renders a <span>, and a span is not a labelable element, so the label/control pair never forms and the visible text is doing nothing." Both hold in the shipped dist (no color-picker module exists; slider.js spreads the rest props onto Root). lib/ds-changes.ts has no entry for either. The DrillArrow entry is the precedent for exactly this shape of gap.
- Suggested: Add two ds-changes.ts entries: publish a ColorField (hex input plus swatch plus picker) in the DS, and make Slider forward id to a labelable element or accept a label slot.

### ds-inconsistency: Three DS components are corrected at every call site and none of the three is in the ledger. DrawerHeader, DrawerBody and DrawerFooter ship mx-auto w-

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:3296-3350 (SectionSheet), :1621-1635 (PickerRow's Checkbox), :2493-2517 (SwitchRow) and :2628-2646 (CheckRow)
- What: Three DS components are corrected at every call site and none of the three is in the ledger. DrawerHeader, DrawerBody and DrawerFooter ship mx-auto w-full max-w-sm and phone shaped padding, so each one carries mx-0 max-w-none plus a width clamp; DS Checkbox ships self-start mt-0.5 and has to be pushed back to self-center on every row; the horizontal Field is a grid with align-items start, so both its cells need self-center before a label lines up with its control. The same overrides are copied into reviews/builder/_option-a, reviews/builder/simplified and reviews/showcase/rail, so this is systemic rather than one screen's taste.
- Evidence: ":3301: "DrawerHeader, DrawerBody and DrawerFooter all ship `mx-auto w-full max-w-sm` and phone-shaped padding, so all three carry the same overrides, and the body additionally needs `min-h-0 grow overflow-y-auto` or the header and footer do not pin." :1622: "DS Checkbox ships `self-start mt-0.5`... which beat the card row's items-center and left the box 14px above centre." :2504: "the horizontal Field is a GRID with align-items start, so its 14px content cell sat at the top of the 20px switch row, 3px above centre, and items-center on the Field lost to the DS's own orientation variant." grep for the DrawerBody override string finds it in four page files. lib/ds-changes.ts has no drawer, che
- Suggested: Add ledger entries: a side-sheet variant of the Drawer parts (or make their width and padding props), an alignment variant on Checkbox, and a vertical alignment option on the horizontal Field. These are the entries that turn repeated per-site patches into one upstream fix.

### copy: The save-blocking alert states an unverified number as product fact: "A hand-picked showcase holds up to 50 reviews. Untick N to save." The 50 comes f

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:2376-2387 (ISSUE_COPY) and MAX_HAND_PICKED at :353
- What: The save-blocking alert states an unverified number as product fact: "A hand-picked showcase holds up to 50 reviews. Untick N to save." The 50 comes from the researcher's clickthrough, not from BrightLocal, and it is one of the two numbers the screen's own report keeps listing as unconfirmed.
- Evidence: The file header, assumption 4: "THE 50-REVIEW CAP on a hand-picked widget is the prototype's number." The report at packages/studio/registries/brightlocal/reports/RM-GET-REVIEWS-AND-WIDGETS-REPORT.md says it twice, in section 4 and again in 4b: "Still unverified, and both are the researcher's numbers, not the brief's: the 50-review cap, and the Yelp exclusion stated as a policy limit."
- Suggested: Get the real ceiling from BrightLocal before the demo, or soften the sentence so it does not quote a number we made up. It is the only hard limit the UI asserts to the user.

### other: Two places where the preview cannot answer a control it offers, on a panel whose whole selling point is that the preview answers everything. The Font 

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/showcase/_option-a/page.jsx:688-700 (FONTS) and :1506-1520 (the columns grid)
- What: Two places where the preview cannot answer a control it offers, on a panel whose whole selling point is that the preview answers everything. The Font select offers seven Google faces and none is loaded, so picking Slabo 27px or Oswald shows a local fallback rather than the face the customer would get. And "Desktop layout display" is applied at every width, because an inline style carries no media query, so a two or three column setting stays two or three columns on a phone.
- Evidence: ":688: "They are Google fonts and nothing in this prototype loads them, so each carries a stack that falls back to something of the same character on a machine without it. The published widget would load the face itself." :1513: "ASSUMPTION: the real control is called 'Desktop layout display', so it presumably drops to one column on a phone. An inline style carries no media query, so the count applies at every width here."
- Suggested: Load the seven faces (a stylesheet link is enough for a prototype) so the Font control demonstrates itself, and drop the grid to one column below the sm breakpoint so the setting behaves like the name it was given.

## Across the app

### copy: The starter's next step still leads with the QR code: "A link on a receipt or a QR code by the till is the fastest start." That is the thing that was 

- Where: /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/components/starter-guide.tsx:46, rendered in the Review Builder card for the starter persona
- What: The starter's next step still leads with the QR code: "A link on a receipt or a QR code by the till is the fastest start." That is the thing that was pulled today, and its CTA lands on a Builder page where the QR route is now hidden.
- Evidence: The hub's own header, page.jsx:74-77: "THE QR BANNER IS OFF (Ali, 20 Sep: 'the QR code was extra from me', and the prototype is now 'like the current project, no extra features')". The Builder's copy of the same flag adds the consequence, /Users/alastairdriver/Development/2026/ramp-ds/gradeui/apps/brightlocal/app/(app)/locations/[location]/reviews/builder/_option-a/page.jsx:1783-1787: "THE QR BANNER IS OFF (Ali, 20 Sep: 'can we hide the QR banner?'). Flip to true to bring it back. It is the only way into the QR generator". To be fair to it, the Builder does still ship a "Receipt QR Link" seed campaign and web link copy ("One link you can put anywhere: on a receipt, in your email signature, o
- Suggested: Lead the step with email or SMS, which the Builder demonstrably does, and hold the QR wording until Ali decides whether the generator comes back.
