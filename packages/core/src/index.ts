/**
 * @grade/core — placeholder entry point.
 *
 * This package exists as a scaffold for extracting design-system foundations
 * (tokens, theme engine primitives, OKLCH utilities, `cn`) out of @grade/ui.
 *
 * Migration plan:
 *   1. Move lib/themes/* from @grade/ui here.
 *   2. Move lib/utils (cn) here.
 *   3. Re-export from @grade/ui so existing consumers keep working.
 *
 * Nothing is exported yet — do not install @grade/core in production code
 * until the first real API lands.
 */
export const __grade_core_placeholder = true as const;
